# install blogdown package
# install.packages("blogdown")
## update Hugo
# blogdown::update_hugo()
#
# Create a new site using Hugo
blogdown::new_site() # this failed because my project was not empty
# Need to create a new project, empty folder and then start using it.
# When you do that, you cannot even save a new file, you have to work from the console
